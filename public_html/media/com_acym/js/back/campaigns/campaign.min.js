jQuery(document).ready(function ($) {

    function Init() {
        setSaleAjax();
    }

    Init();

    function setSaleAjax() {
        let $saleContainers = $('.acym__campaign__sale');

        if ($saleContainers.length === 0) return;

        $saleContainers.each($oneContainer => {
            console.log($oneContainer);
        });
    }
});